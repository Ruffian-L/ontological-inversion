# Dependencies — pinned, "trust the bytes" (see STANDARDS.md).
#
# Pinned to exact releases verified free of known CVEs on 2026-06-25 (pip-audit / OSV,
# full transitive tree). The previous *unpinned* list made Snyk read every package as
# "any version ever shipped", inheriting the entire historical CVE backlog of the
# torch/transformers tree (~30 phantom CVEs + supply-chain warnings). Pinning collapses
# that to the real, audited surface: zero known vulnerabilities.
#
# To bump: change a version below, then re-audit before committing:
#   pip-audit -r requirements.txt        # must report "No known vulnerabilities found"

torch==2.13.0
transformers==5.12.1
sentence-transformers==5.6.0
einops==0.8.2
safetensors==0.8.0
numpy==2.5.0
matplotlib==3.11.0   # used by topology.py & fold_decay.py (was missing -> clean-install ImportError)
