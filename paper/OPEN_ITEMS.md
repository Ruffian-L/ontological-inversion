# Open items

## Submission blockers

1. **Publication metadata.** Name and affiliation are now Jason’s: Jason Van
   Pham (ruffian-l), independent researcher, no institution. Gemini, Grok,
   ChatGPT, and Claude remain the named AI research collaborators. Contact
   details were not supplied and are not invented.
2. **Human scientific proof.** Jason should read the complete 5,280-word paper
   for emphasis and voice, then choose a venue and template. Citation keys can
   then be converted to the venue style.
3. **Priority packet export.** Export paper/PRIORITY_RECORD.md together with the
   discovery ledger, challenge ledger, paper/ORIGINAL_REPO_MANIFEST.md, dated
   logs, hashes, and the final PDF. The original Git repository has now been
   recovered; finding it is no longer an open item.

## Available but not required for this paper

- The Qwen3-Embedding-8B-Q4_K_M file is present at
  /home/ruffianl/Hub/AI/Models/Qwen3-Embedding-8B-Q4_K_M.gguf and the embedding
  server is currently running it on port 8081. This paper does not claim a
  matched Q4 replication because no separately labeled E10-Q4 result is included.
- Add blinded semantic review of the small oracle and E10 transcripts while
  retaining the frozen E10 rubric as its primary decision.
- Repeat the rank and subspace curves over multiple seeded splits to add
  uncertainty bands without replacing the present receipts.
- Convert the Markdown references to the selected venue’s BibTeX style.
- Recover the original adapter training corpus if a further archive contains it.
  The trainer source and structured input specification are present, but the
  corpus required for byte-identical retraining is still absent.

## Future experiments

- Train a stronger bridge with a generator-aware objective on calibration
  memories, then evaluate once on untouched memories.
- Freeze an automatic gain selector on calibration memories and permit abstention
  when no stable interval exists.
- Test multiple simultaneous memories at a fixed total slot budget, measuring
  cross-binding and unsupported recombination.
- Expand the memory typology to dates, names, numbers, negation, relations,
  preferences, and procedures.
- Run encoder × target family combinations without treating equal hidden width
  as coordinate compatibility.
- Execute E6’s name-frequency × redundancy design as preregistered.

None of these future experiments is required to preserve the already established
Nomic-to-Qwen steering, Qwen3-to-Llama semantic transmission, target-space
reconstruction, ordinary contextual use, or inference demonstrations.
